<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.facebook.post.dao.PostDAO">
 	<!-- 글쓰기 insert -->
 	<insert id="insertPost" parameterType="map">
 		INSERT INTO `post`
		(
			`userId`
			,`content`
			,`imagePath`
			,`createdAt`
			,`updatedAt`
		)
		VALUES
		(
			#{userId}
			,#{content}
			,#{imagePath}
			,NOW()
			,NOW()
		)
 	</insert>
 	
 	<!-- 글 목록 select -->
 	<select id="selectPostList" resultType="com.facebook.post.model.Post">
		SELECT
			`id`
			,`userId`
			,`content`
			,`imagePath`
			,`createdAt`
			,`updatedAt`
		FROM
			`post`
		ORDER BY `id` DESC
	</select>
	
	<!-- 글 하나의 postId -->
	<select id="selectPostById" parameterType="int" resultType="com.facebook.post.model.Post">
		SELECT
			`id`
			,`userId`
			,`content`
			,`imagePath`
			,`createdAt`
			,`updatedAt`
		FROM
			`post`
		WHERE
			`id` = #{id}
	</select>
 </mapper>